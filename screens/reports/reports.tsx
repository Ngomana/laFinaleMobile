import * as React from "react";
import { Text, View, StyleSheet, SafeAreaView } from "react-native";

interface ReportScrrenProps {}

const ReportScrren = (props: ReportScrrenProps) => {
  return (
    <SafeAreaView>
      <Text>ReportScrren</Text>
    </SafeAreaView>
  );
};

export default ReportScrren;

const styles = StyleSheet.create({
  container: {},
});
